<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Listing</title>
</head>
<body>

<form method="POST" action="/listings/<%= listing._id %>?_method=PUT">

    <input name="listing[title]" value="<%= listing.title %>" required>
    <br><br>

    <textarea name="listing[description]"><%= listing.description %></textarea>
    <br><br>

    <input name="listing[image]" value="<%= listing.image %>">
    <br><br>

    <input type="number" name="listing[price]" value="<%= listing.price %>">
    <br><br>

    <input name="listing[country]" value="<%= listing.country %>">
    <br><br>

    <input name="listing[location]" value="<%= listing.location %>">
    <br><br>

    <button>Update</button>

</form>

</body>
</html>
